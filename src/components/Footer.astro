---
import { t, type Locale } from '../i18n/ui';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const copy = t(locale);
---

<footer class="site-footer">
  <div class="wrap">
    <div>{copy.footer.line}</div>
    <div>{copy.footer.note}</div>
  </div>
</footer>
